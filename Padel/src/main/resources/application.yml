spring:
  datasource:
    url: jdbc:mysql://localhost:6606/wigell_padel_db?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8590/realms/keycloak"

  keycloak:
    server-url: http://localhost:8590
    realm: keycloak

  keycloak.admin:
    realm: keycloak
    client-id: padel-admin
    client-secret: N2xk8m4ii78vRqp456JHYZ01CdefWstU

  application:
    name: WigellPadel


management:
  endpoints:
    web:
      exposure:
        include: health, info
      base-path: /actuator
  endpoint:
    health:
      show-details: always

server:
  port: 8584

logging:
  level:
    root: INFO
    com.strom.wigellPadel: DEBUG

converter:
  service:
    url: http://localhost:8700/api/convert
internal:
  api:
    key: 123456789
